# -*- coding: utf-8 -*-
"""disease dataset

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/13IKx_QgEc3mdfFrzSK6ALNp7MTFINZlP
"""

import os
os.environ['KAGGLE_USERNAME'] = 'laks'
os.environ['KAGGLE_KEY'] = 'KGAT_46624475f47bc83469575daad5c3d273'

!pip install kaggle

!kaggle datasets download -d dhivyeshrk/diseases-and-symptoms-dataset

!unzip /content/diseases-and-symptoms-dataset.zip -d /content/disease

import pandas as pd
df = pd.read_csv("/content/diseases-and-symptoms-dataset.zip")
df.head(10)

df.tail(10)

print(len(df))

print(df.shape)

df.info()

df.describe()

df.duplicated().sum()

df.dtypes

print(df.columns)

print(df['insomnia'])

df.drop('dizziness', axis=1, inplace=True)
print(df)

df.isnull().sum()

for col in df.columns:
    if df[col].dtype == 'object':
        df[col].fillna(df[col].mode()[0], inplace=True)
    else:
        df[col].fillna(0, inplace=True)

df.drop_duplicates(inplace=True)
print(df)

df.head()
df.info()

df.isnull().sum()

import matplotlib.pyplot as plt

df['diseases'].value_counts().head(10).plot(kind='bar')

plt.title("Top 10 Diseases")
plt.xlabel("Disease")
plt.ylabel("Count")
plt.show()

X = df.drop('diseases', axis=1)
y = df['diseases']

from sklearn.model_selection import train_test_split

X_train, X_test, y_train, y_test = train_test_split(
    X,
    y,
    test_size=0.3,
    random_state=42
)

from sklearn.linear_model import LogisticRegression

model = LogisticRegression(max_iter=2000)

model.fit(X_train, y_train)

print("Model Trained")

y_pred = model.predict(X_test)

from sklearn.metrics import accuracy_score
from sklearn.metrics import confusion_matrix
from sklearn.metrics import classification_report

print("Accuracy :", accuracy_score(y_test, y_pred))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred))

print("\nClassification Report:")
print(classification_report(y_test, y_pred))